name: "Manual Remote Merge: Dev -> Pre"

on:
  workflow_dispatch:
    inputs:
      target-repo:
        description: 'Репозиторій для синхронізації (напр. chTestORG/ui-app)'
        required: true
        default: 'chTestORG/org-front-end'

jobs:
  merge-check-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Checkout Target Repository
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.target-repo }}
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}
          path: target-repo

      - name: Check for Changes & Merge
        id: check_changes
        working-directory: ./target-repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 1. Завантажуємо актуальний стан обох гілок
          git fetch origin develop prerelease

          # 2. ПЕРЕВІРКА: Використовуємо origin/ для обох гілок (це вирішує твою помилку)
          CHANGES_COUNT=$(git rev-list --count origin/prerelease..origin/develop)
          
          echo "Found $CHANGES_COUNT new commits in develop relative to prerelease."
          
          if [ "$CHANGES_COUNT" -eq "0" ]; then
            echo "No changes to merge. Skipping PR creation."
            echo "has_changes=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "has_changes=true" >> $GITHUB_OUTPUT

          # 3. ПІДГОТОВКА: Створюємо локальну prerelease з віддаленої
          git checkout -B prerelease origin/prerelease
          
          # 4. СТВОРЕННЯ ГІЛКИ: Створюємо тимчасову гілку від prerelease
          git branch -D merge-develop-into-prerelease || true
          git checkout -b merge-develop-into-prerelease
          
          # 5. МЕРДЖ: Вливаємо develop
          if git merge origin/develop; then
            git push origin merge-develop-into-prerelease --force
          else
            echo "❌ Merge conflict detected! Stopping."
            exit 1
          fi

      - name: Create Pull Request
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const [owner, repo] = "${{ inputs.target-repo }}".split('/');
            try {
              const { data: pullRequest } = await github.rest.pulls.create({
                owner: owner,
                repo: repo,
                title: '🚀 Merge: develop -> prerelease',
                head: 'merge-develop-into-prerelease',
                base: 'prerelease',
                body: 'Автоматично створений PR для синхронізації змін між гілками.',
              });
              core.info(`PR created: ${pullRequest.html_url}`);
            } catch (error) {
              if (error.message.includes('A pull request already exists')) {
                core.info('PR вже існує, гілку оновлено.');
              } else {
                core.setFailed(`API Error: ${error.message}`);
              }
            }